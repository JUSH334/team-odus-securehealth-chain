{
  "name": "securehealth-chain",
  "version": "0.7.0",
  "description": "Blockchain-based insurance claim platform",
  "scripts": {
    "test": "hardhat test",
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.js --network localhost",
    "node": "hardhat node",
    "cli": "node scripts/patient-cli.js"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^3.0.0"
  },
  "dependencies": {
    "readline": "^1.3.0"
  }
}
